# xAI Remaining environment template.
# Copy these values into Windows user/system environment variables.
# Do not commit real credentials.

XAI_MGMT_KEY=
XAI_TEAM_ID=

# Optional parser overrides. Defaults are shown for troubleshooting only.
# Most users should leave these unset.
# XAI_PREPAID_TOTAL_FIELD_PATH=total.val
# XAI_PREPAID_USED_FIELD_PATH=coreInvoice.prepaidCreditsUsed.val
